name: Build TR4QT

on:
  push:
    tags:
      - 'v*'       # Release tags: v2.90.1
      - 'v*-beta*' # Beta releases: v2.91.0-beta1
      - 'v*-rc*'   # Release candidates: v2.91.0-rc1
      - 'test-*'   # Test builds: test-needs-widget
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:
    inputs:
      build_windows:
        description: 'Build Windows'
        required: false
        default: 'true'
        type: boolean
      build_macos:
        description: 'Build macOS'
        required: false
        default: 'true'
        type: boolean
      build_linux:
        description: 'Build Linux ARM64'
        required: false
        default: 'true'
        type: boolean

env:
  QT_VERSION: '6.7.2'
  HAMLIB_VERSION: '4.5.5'

permissions:
  contents: write

jobs:
  build-windows:
    name: Windows (MinGW)
    runs-on: windows-latest
    if: github.event_name != 'workflow_dispatch' || inputs.build_windows == true

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ env.QT_VERSION }}
        host: windows
        target: desktop
        arch: win64_mingw
        tools: 'tools_mingw1310'
        modules: 'qtwebsockets qthttpserver'  # Additional modules (HttpServer depends on WebSockets)
        cache: true

    - name: Download Hamlib
      shell: bash
      run: |
        curl -L -o hamlib.zip "https://github.com/Hamlib/Hamlib/releases/download/${{ env.HAMLIB_VERSION }}/hamlib-w64-${{ env.HAMLIB_VERSION }}.zip"
        unzip hamlib.zip -d hamlib-temp
        # Find the extracted directory (it may have version in name)
        HAMLIB_DIR=$(ls -d hamlib-temp/hamlib-w64-* 2>/dev/null | head -1)
        if [ -z "$HAMLIB_DIR" ]; then
          HAMLIB_DIR="hamlib-temp"
        fi
        mkdir -p C:/hamlib
        cp -r "$HAMLIB_DIR"/* C:/hamlib/ || cp -r hamlib-temp/* C:/hamlib/
        ls -la C:/hamlib/
        ls -la C:/hamlib/bin/ || true
        ls -la C:/hamlib/lib/ || true

    - name: Configure CMake
      shell: bash
      run: |
        export PATH="${{ runner.temp }}/../Qt/Tools/mingw1310_64/bin:$PATH"
        cmake -B build -G "MinGW Makefiles" \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_PREFIX_PATH="${{ env.QT_ROOT_DIR }}" \
          -DHAMLIB_ROOT="C:/hamlib"

    - name: Build
      shell: bash
      run: |
        export PATH="${{ runner.temp }}/../Qt/Tools/mingw1310_64/bin:$PATH"
        cmake --build build --config Release -j4

    - name: Run Tests
      shell: bash
      run: |
        export PATH="${{ runner.temp }}/../Qt/Tools/mingw1310_64/bin:${{ env.QT_ROOT_DIR }}/bin:C:/hamlib/bin:$PATH"
        cd build
        ctest --output-on-failure -C Release
      continue-on-error: true

    - name: Deploy Qt Dependencies
      shell: bash
      run: |
        echo "=== Explicit Qt Deployment (no windeployqt) ==="

        QT_BIN="${{ env.QT_ROOT_DIR }}/bin"
        QT_PLUGINS="${{ env.QT_ROOT_DIR }}/plugins"
        MINGW_BIN="${{ runner.temp }}/../Qt/Tools/mingw1310_64/bin"
        DEST="build/src"

        # Copy Qt DLLs (explicit list of what we need)
        echo "Copying Qt DLLs..."
        cp "$QT_BIN/Qt6Core.dll" "$DEST/"
        cp "$QT_BIN/Qt6Gui.dll" "$DEST/"
        cp "$QT_BIN/Qt6Widgets.dll" "$DEST/"
        cp "$QT_BIN/Qt6Network.dll" "$DEST/"
        cp "$QT_BIN/Qt6Sql.dll" "$DEST/"
        cp "$QT_BIN/Qt6HttpServer.dll" "$DEST/"
        cp "$QT_BIN/Qt6WebSockets.dll" "$DEST/"
        cp "$QT_BIN/Qt6PrintSupport.dll" "$DEST/"
        cp "$QT_BIN/Qt6Concurrent.dll" "$DEST/"

        # Copy Qt plugins
        echo "Copying Qt plugins..."
        mkdir -p "$DEST/platforms"
        cp "$QT_PLUGINS/platforms/qwindows.dll" "$DEST/platforms/"

        mkdir -p "$DEST/styles"
        cp "$QT_PLUGINS/styles/qwindowsvistastyle.dll" "$DEST/styles/" || echo "WARNING: qwindowsvistastyle.dll not found (optional)"

        # Copy SQL drivers (CRITICAL - required for database access)
        echo "Copying SQL drivers..."
        mkdir -p "$DEST/sqldrivers"
        echo "DEBUG: QT_PLUGINS=$QT_PLUGINS"
        echo "DEBUG: Looking for qsqlite.dll in: $QT_PLUGINS/sqldrivers/"
        ls -la "$QT_PLUGINS/sqldrivers/" || echo "ERROR: sqldrivers directory not found!"
        if [ -f "$QT_PLUGINS/sqldrivers/qsqlite.dll" ]; then
          cp "$QT_PLUGINS/sqldrivers/qsqlite.dll" "$DEST/sqldrivers/"
          echo "SUCCESS: qsqlite.dll copied"
        else
          echo "ERROR: qsqlite.dll not found at $QT_PLUGINS/sqldrivers/qsqlite.dll"
          echo "ERROR: This is a FATAL error - application will not work without SQL driver"
          exit 1
        fi

        # Copy Hamlib DLLs
        echo "Copying Hamlib DLLs..."
        cp C:/hamlib/bin/*.dll "$DEST/" || true

        # Copy MinGW runtime DLLs
        echo "Copying MinGW runtime DLLs..."
        cp "$MINGW_BIN/libgcc_s_seh-1.dll" "$DEST/" || cp "$MINGW_BIN/libgcc_s_dw2-1.dll" "$DEST/" || cp "$MINGW_BIN/libgcc_s_sjlj-1.dll" "$DEST/" || echo "WARNING: No libgcc variant found"
        cp "$MINGW_BIN/libstdc++-6.dll" "$DEST/" || echo "WARNING: libstdc++-6.dll not found"
        cp "$MINGW_BIN/libwinpthread-1.dll" "$DEST/" || echo "WARNING: libwinpthread-1.dll not found"

        # Verify deployment
        echo "=== Deployment Verification ==="
        echo "Qt DLLs:"
        ls -lh "$DEST"/Qt6*.dll | awk '{print "  " $9, $5}'
        echo "SQL Drivers:"
        if [ -d "$DEST/sqldrivers" ] && ls "$DEST/sqldrivers/"*.dll >/dev/null 2>&1; then
          ls -lh "$DEST/sqldrivers/"*.dll | awk '{print "  " $9, $5}'
        else
          echo "  ERROR: No SQL drivers found!"
          exit 1
        fi
        echo "Platforms:"
        ls -lh "$DEST/platforms/"*.dll | awk '{print "  " $9, $5}'
        echo "MinGW Runtime:"
        ls -lh "$DEST"/lib*.dll | awk '{print "  " $9, $5}'

    - name: Create Installer
      shell: bash
      run: |
        # Install NSIS
        choco install nsis -y
        export PATH="/c/Program Files (x86)/NSIS:$PATH"

        # Build installer
        cd installer
        makensis tr4qt.nsi

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TR4QT-Windows
        path: |
          build/src/tr4qt.exe
          build/src/*.dll
          build/src/platforms/
          build/src/imageformats/
          build/src/styles/
          build/src/tls/
          build/src/TR4QT-*-Setup.exe

  build-macos:
    name: macOS
    runs-on: macos-latest
    if: github.event_name != 'workflow_dispatch' || inputs.build_macos == true

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: |
        brew install qt@6 libusb
        echo "Qt6_DIR=$(brew --prefix qt@6)/lib/cmake/Qt6" >> $GITHUB_ENV

    - name: Build Hamlib from source (unsigned)
      run: |
        curl -L -o hamlib.tar.gz "https://github.com/Hamlib/Hamlib/releases/download/${{ env.HAMLIB_VERSION }}/hamlib-${{ env.HAMLIB_VERSION }}.tar.gz"
        tar -xzf hamlib.tar.gz
        cd hamlib-${{ env.HAMLIB_VERSION }}
        ./configure --prefix=$HOME/hamlib-unsigned --disable-static
        make -j$(sysctl -n hw.ncpu)
        make install
        echo "HAMLIB_ROOT=$HOME/hamlib-unsigned" >> $GITHUB_ENV

    - name: Configure CMake
      run: |
        cmake -B build \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_PREFIX_PATH="$(brew --prefix qt@6)" \
          -DHAMLIB_ROOT="$HOME/hamlib-unsigned"

    - name: Build
      run: cmake --build build --config Release -j$(sysctl -n hw.ncpu)

    - name: Run Tests
      run: |
        cd build
        ctest --output-on-failure -C Release
      continue-on-error: true

    - name: Create App Bundle
      run: |
        cd build/src

        # Copy unsigned Hamlib dylib into the app bundle
        mkdir -p tr4qt.app/Contents/Frameworks
        cp $HOME/hamlib-unsigned/lib/libhamlib.*.dylib tr4qt.app/Contents/Frameworks/ || true

        # Bundle Qt frameworks and plugins with macdeployqt
        $(brew --prefix qt@6)/bin/macdeployqt tr4qt.app -verbose=2

        # Copy missing dependencies that macdeployqt didn't bundle
        cp /opt/homebrew/opt/brotli/lib/libbrotlicommon.1.dylib tr4qt.app/Contents/Frameworks/
        cp -R /opt/homebrew/opt/qtbase/lib/QtDBus.framework tr4qt.app/Contents/Frameworks/
        cp /opt/homebrew/opt/dbus/lib/libdbus-1.3.dylib tr4qt.app/Contents/Frameworks/

        # Fix library paths to use @rpath
        for lib in tr4qt.app/Contents/Frameworks/libhamlib.*.dylib; do
          if [ -f "$lib" ]; then
            install_name_tool -id "@rpath/$(basename $lib)" "$lib"
          fi
        done

        # Fix manually copied libraries to use @rpath
        if [ -f "tr4qt.app/Contents/Frameworks/libbrotlicommon.1.dylib" ]; then
          install_name_tool -id "@rpath/libbrotlicommon.1.dylib" tr4qt.app/Contents/Frameworks/libbrotlicommon.1.dylib
        fi

        if [ -f "tr4qt.app/Contents/Frameworks/libdbus-1.3.dylib" ]; then
          install_name_tool -id "@rpath/libdbus-1.3.dylib" tr4qt.app/Contents/Frameworks/libdbus-1.3.dylib
        fi

        if [ -f "tr4qt.app/Contents/Frameworks/QtDBus.framework/Versions/A/QtDBus" ]; then
          install_name_tool -id "@rpath/QtDBus.framework/Versions/A/QtDBus" tr4qt.app/Contents/Frameworks/QtDBus.framework/Versions/A/QtDBus
          # Fix QtDBus dependency on libdbus to use @rpath instead of absolute path
          install_name_tool -change "/opt/homebrew/opt/dbus/lib/libdbus-1.3.dylib" "@rpath/libdbus-1.3.dylib" tr4qt.app/Contents/Frameworks/QtDBus.framework/Versions/A/QtDBus
        fi

        # Update executable to look for libraries in @rpath
        install_name_tool -add_rpath "@executable_path/../Frameworks" tr4qt.app/Contents/MacOS/tr4qt || true

        # Sign everything with ad-hoc signatures
        # Sign all dylibs in Frameworks
        for lib in tr4qt.app/Contents/Frameworks/*.dylib; do
          if [ -f "$lib" ]; then
            codesign -s - -f "$lib"
          fi
        done

        # Sign all Qt frameworks
        for framework in tr4qt.app/Contents/Frameworks/*.framework; do
          if [ -d "$framework" ]; then
            codesign -s - -f "$framework"
          fi
        done

        # Sign all plugins
        find tr4qt.app/Contents/PlugIns -name "*.dylib" -exec codesign -s - -f {} \;

        # Sign the executable
        codesign -s - -f tr4qt.app/Contents/MacOS/tr4qt

        # Sign the entire app bundle
        codesign -s - -f tr4qt.app

        # Create DMG manually with hdiutil (macdeployqt -dmg breaks signatures)
        mkdir -p dmg-contents
        cp -R tr4qt.app dmg-contents/
        hdiutil create -volname "TR4QT" -srcfolder dmg-contents -ov -format UDZO tr4qt.dmg
        rm -rf dmg-contents

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TR4QT-macOS
        path: build/src/tr4qt.dmg

  # Commented out: Linux x86_64 is not a priority (Raspberry Pi ARM64 is the Linux focus)
  # build-linux-x86_64:
  #   name: Linux (x86_64)
  #   runs-on: ubuntu-24.04
  #
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #
  #   - name: Install Dependencies
  #     run: |
  #       sudo apt-get update
  #       sudo apt-get install -y \
  #         build-essential \
  #         cmake \
  #         libgl1-mesa-dev \
  #         libhamlib-dev \
  #         qt6-base-dev \
  #         qt6-websockets-dev \
  #         qt6-httpserver-dev \
  #         libqt6sql6-sqlite \
  #         libqt6network6 \
  #         libfuse2
  #
  #   - name: Configure CMake
  #     run: |
  #       cmake -B build \
  #         -DCMAKE_BUILD_TYPE=Release \
  #         -DCMAKE_INSTALL_PREFIX=/usr
  #
  #   - name: Build
  #     run: cmake --build build --config Release -j$(nproc)
  #
  #   - name: Run Tests
  #     run: |
  #       cd build
  #       ctest --output-on-failure -C Release
  #     continue-on-error: true
  #
  #   - name: Install to AppDir
  #     run: |
  #       DESTDIR=$PWD/appdir cmake --install build
  #       mkdir -p appdir/usr/share/applications
  #       mkdir -p appdir/usr/share/icons/hicolor/256x256/apps
  #
  #       # Create desktop entry
  #       cat > appdir/usr/share/applications/tr4qt.desktop <<EOF
  #       [Desktop Entry]
  #       Type=Application
  #       Name=TR4QT
  #       Comment=Amateur Radio Contest Logger
  #       Exec=tr4qt
  #       Icon=tr4qt
  #       Categories=HamRadio;Network;
  #       EOF
  #
  #   - name: Create AppImage
  #     run: |
  #       wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
  #       wget -q https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
  #       chmod +x linuxdeploy*.AppImage
  #
  #       export QMAKE=/usr/lib/qt6/bin/qmake
  #       export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
  #
  #       ./linuxdeploy-x86_64.AppImage \
  #         --appdir appdir \
  #         --plugin qt \
  #         --output appimage
  #
  #   - name: Upload Artifact
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: TR4QT-Linux-x86_64
  #       path: TR4QT*.AppImage

  build-linux-arm64:
    name: Linux (ARM64/RPi)
    runs-on: ubuntu-latest
    if: github.event_name != 'workflow_dispatch' || inputs.build_linux == true
    continue-on-error: true  # Qt HttpServer not in Ubuntu repos yet

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3
      with:
        platforms: arm64

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build ARM64 in Docker
      run: |
        docker run --rm --platform linux/arm64 \
          -v $PWD:/workspace \
          -w /workspace \
          arm64v8/ubuntu:24.04 \
          bash -c "
            set -e
            echo '==> Explicit Linux ARM64 Deployment (no linuxdeploy)'

            # Install dependencies
            apt-get update
            DEBIAN_FRONTEND=noninteractive apt-get install -y \
              build-essential cmake libgl1-mesa-dev libhamlib-dev \
              qt6-base-dev qt6-websockets-dev qt6-httpserver-dev \
              libqt6sql6-sqlite libqt6network6 libfuse2 wget file patchelf

            # Build
            echo '==> Building TR4QT...'
            cmake -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr
            cmake --build build --config Release -j\$(nproc)

            # Create AppDir structure
            echo '==> Creating AppDir structure...'
            mkdir -p appdir/usr/bin appdir/usr/lib appdir/usr/share/applications
            mkdir -p appdir/usr/plugins/platforms appdir/usr/plugins/sqldrivers

            # Copy executable
            cp build/src/tr4qt appdir/usr/bin/

            # Copy Qt libraries explicitly
            echo '==> Copying Qt libraries...'
            QT_LIBS=(
              libQt6Core.so.6 libQt6Gui.so.6 libQt6Widgets.so.6
              libQt6Network.so.6 libQt6Sql.so.6 libQt6HttpServer.so.6
              libQt6WebSockets.so.6 libQt6PrintSupport.so.6
              libQt6Concurrent.so.6 libQt6DBus.so.6
            )
            for lib in \${QT_LIBS[@]}; do
              cp /usr/lib/aarch64-linux-gnu/\$lib appdir/usr/lib/ || echo \"WARNING: \$lib not found\"
            done

            # Copy Qt plugins
            echo '==> Copying Qt plugins...'
            cp /usr/lib/aarch64-linux-gnu/qt6/plugins/platforms/libqxcb.so appdir/usr/plugins/platforms/
            cp /usr/lib/aarch64-linux-gnu/qt6/plugins/sqldrivers/libqsqlite.so appdir/usr/plugins/sqldrivers/

            # Copy Hamlib and other system libs
            echo '==> Copying system libraries...'
            cp /usr/lib/aarch64-linux-gnu/libhamlib.so.4 appdir/usr/lib/ || true

            # Create desktop entry
            cat > appdir/usr/share/applications/tr4qt.desktop <<'DESKTOP_EOF'
        [Desktop Entry]
        Type=Application
        Name=TR4QT
        Comment=Amateur Radio Contest Logger
        Exec=tr4qt
        Icon=tr4qt
        Categories=HamRadio;Network;
        DESKTOP_EOF

            # Create AppRun script
            cat > appdir/AppRun <<'APPRUN_EOF'
        #!/bin/bash
        SELF=\$(readlink -f \"\$0\")
        HERE=\${SELF%/*}
        export LD_LIBRARY_PATH=\"\${HERE}/usr/lib:\$LD_LIBRARY_PATH\"
        export QT_PLUGIN_PATH=\"\${HERE}/usr/plugins\"
        exec \"\${HERE}/usr/bin/tr4qt\" \"\$@\"
        APPRUN_EOF
            chmod +x appdir/AppRun

            # Create AppImage using appimagetool
            echo '==> Creating AppImage...'
            # Download ARM64 appimagetool (runs inside ARM64 Docker container)
            wget -q https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-aarch64.AppImage
            chmod +x appimagetool-aarch64.AppImage
            # Extract and run directly (avoids FUSE requirement in Docker)
            ./appimagetool-aarch64.AppImage --appimage-extract >/dev/null
            ARCH=aarch64 ./squashfs-root/AppRun appdir TR4QT-ARM64.AppImage

            echo '==> Deployment complete'
            ls -lh TR4QT-ARM64.AppImage
          "

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TR4QT-Linux-ARM64
        path: TR4QT*.AppImage

  # Create release when a tag is pushed
  release:
    name: Create Release
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Display structure
      run: ls -R artifacts

    - name: Create Windows ZIP
      run: |
        cd artifacts/TR4QT-Windows
        zip -r ../TR4QT-${{ github.ref_name }}-Windows.zip .

    - name: Rename Windows Installer
      run: |
        mv artifacts/TR4QT-Windows/TR4QT-*-Setup.exe artifacts/TR4QT-${{ github.ref_name }}-Setup.exe || true

    - name: Rename macOS DMG
      run: |
        mv artifacts/TR4QT-macOS/tr4qt.dmg artifacts/TR4QT-${{ github.ref_name }}-macOS.dmg

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/TR4QT-${{ github.ref_name }}-Windows.zip
          artifacts/TR4QT-${{ github.ref_name }}-Setup.exe
          artifacts/TR4QT-${{ github.ref_name }}-macOS.dmg
        draft: ${{ !contains(github.ref_name, '-rc') }}
        prerelease: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Upload test artifacts for test-* tags (no release created)
  test-artifacts:
    name: Upload Test Artifacts
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/test-')

    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Display structure
      run: ls -R artifacts

    - name: Create Windows ZIP
      run: |
        cd artifacts/TR4QT-Windows
        zip -r ../TR4QT-${{ github.ref_name }}-Windows.zip .

    - name: Rename macOS DMG
      run: |
        mv artifacts/TR4QT-macOS/tr4qt.dmg artifacts/TR4QT-${{ github.ref_name }}-macOS.dmg

    - name: Upload combined test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: test-build-${{ github.ref_name }}
        path: |
          artifacts/TR4QT-${{ github.ref_name }}-Windows.zip
          artifacts/TR4QT-${{ github.ref_name }}-macOS.dmg
        retention-days: 30

  notify:
    name: Slack Notification
    needs: [build-windows, build-macos, build-linux-arm64]
    runs-on: ubuntu-latest
    if: always()  # Run even if builds fail

    steps:
    - name: Determine build status
      id: status
      run: |
        if [ "${{ needs.build-windows.result }}" == "success" ] && \
           [ "${{ needs.build-macos.result }}" == "success" ] && \
           [ "${{ needs.build-linux-arm64.result }}" == "success" ]; then
          echo "status=✅ SUCCESS" >> $GITHUB_OUTPUT
          echo "color=good" >> $GITHUB_OUTPUT
        else
          echo "status=❌ FAILED" >> $GITHUB_OUTPUT
          echo "color=danger" >> $GITHUB_OUTPUT
        fi

    - name: Send Slack notification
      run: |
        curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
          -H 'Content-Type: application/json' \
          -d '{
            "attachments": [{
              "color": "${{ steps.status.outputs.color }}",
              "title": "TR4QT Build ${{ steps.status.outputs.status }}",
              "text": "Build triggered by ${{ github.event_name }}",
              "fields": [
                {
                  "title": "Windows",
                  "value": "${{ needs.build-windows.result }}",
                  "short": true
                },
                {
                  "title": "macOS",
                  "value": "${{ needs.build-macos.result }}",
                  "short": true
                },
                {
                  "title": "Linux ARM64",
                  "value": "${{ needs.build-linux-arm64.result }}",
                  "short": true
                },
                {
                  "title": "Branch/Tag",
                  "value": "${{ github.ref_name }}",
                  "short": true
                },
                {
                  "title": "Commit",
                  "value": "<${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}|${{ github.sha }}>",
                  "short": false
                },
                {
                  "title": "Build URL",
                  "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Build>",
                  "short": false
                }
              ],
              "footer": "GitHub Actions",
              "ts": '${{ github.event.head_commit.timestamp }}'
            }]
          }'
