---
description: Run pre-release checklist, commit changes, and push to GitHub
allowed-tools: Bash(git add:*), Bash(git commit:*), Bash(git push:*)
---
1. Run the pre-release checklist for this project and verify all steps are complete.
2. Update the changes document.
3. If successful, stage all current changes using `git add .`.
4. Generate a concise, meaningful commit message based on the changes and commit them.
5. Push the current branch to the 'origin' remote on GitHub.
